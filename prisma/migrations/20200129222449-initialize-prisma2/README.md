# Migration `20200129222449-initialize-prisma2`

This migration has been generated by Simon Jespersen at 1/29/2020, 10:24:49 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    "email" text  NOT NULL DEFAULT '',
    "github" text  NOT NULL DEFAULT '',
    "id" text  NOT NULL ,
    "linkedin" text  NOT NULL DEFAULT '',
    "location" text  NOT NULL DEFAULT '',
    "name" text  NOT NULL DEFAULT '',
    "stackoverflow" text  NOT NULL DEFAULT '',
    "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Company" (
    "active" boolean  NOT NULL DEFAULT false,
    "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    "id" text  NOT NULL ,
    "logoFile" text  NOT NULL DEFAULT '',
    "name" text  NOT NULL DEFAULT '',
    "period" text  NOT NULL DEFAULT '',
    "position" text  NOT NULL DEFAULT '',
    "startDate" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."PlaygroundEquipment" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    "demoLink" text  NOT NULL DEFAULT '',
    "icon" text  NOT NULL DEFAULT '',
    "id" text  NOT NULL ,
    "name" text  NOT NULL DEFAULT '',
    "repositoryLink" text  NOT NULL DEFAULT '',
    "tags" text []  ,
    "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Tool" (
    "id" SERIAL,
    "name" text  NOT NULL DEFAULT '',
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "Tool.name" ON "public"."Tool"("name")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200129222449-initialize-prisma2
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,50 @@
+
+datasource db {
+  provider = "postgresql"
+  url      = env("POSTGRES_URL")
+
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id            String   @id @default(cuid())
+  name          String
+  email         String   @unique
+  location      String
+  github        String
+  stackoverflow String
+  linkedin      String
+  createdAt     DateTime @default(now())
+  updatedAt     DateTime @updatedAt
+}
+
+model Company {
+  id        String   @id @default(cuid())
+  logoFile  String
+  name      String
+  position  String
+  period    String
+  active    Boolean
+  startDate DateTime
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
+model PlaygroundEquipment {
+  id             String   @id @default(cuid())
+  name           String
+  repositoryLink String
+  demoLink       String
+  icon           String
+  tags           String[]
+  createdAt      DateTime @default(now())
+  updatedAt      DateTime @updatedAt
+}
+
+model Tool {
+  id   Int    @id @default(autoincrement())
+  name String @unique
+}
```


