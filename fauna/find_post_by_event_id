Query(
  Lambda(
    "eventId", 
    Select(
      ["data"],
      Map(
        Filter(
          Paginate(Match(Index("allPosts"))),
          Lambda(
            "post",
            Equals("256379156296630802", Select(["data", "event", "id"], Get(Var("post"))))
          )
        ),
        Lambda(
          "post",
          Get(Var("post"))  
        )
      )
    )
  )
)